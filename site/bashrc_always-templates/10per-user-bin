# If the user has a "~/bin"-directory, prepend it to the user's $PATH unless already there.
{
	test -n "$HOME" && test -e "$HOME/bin" \
	&& test "`
		stat -Lc '%A %u %g' "$HOME/bin" | cut -c1,4-
	`" = "dx------ `id -u` `id -g`"
} && export PATH="$HOME/bin:$PATH" || true
