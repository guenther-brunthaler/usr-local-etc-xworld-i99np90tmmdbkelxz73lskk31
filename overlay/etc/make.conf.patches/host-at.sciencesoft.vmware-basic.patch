Define basic settings for "vmware.sciencesoft.at".

Index: etc/make.conf
===================================================================
--- etc.orig/make.conf
+++ etc/make.conf
@@ -33,7 +33,7 @@ USE="
 # Change this line as appropriate (i686, i586, i486 or i386).
 # All modern systems (even Athlons) should use "i686-pc-linux-gnu".
 # All K6's are i586.
-#CHOST="i686-pc-linux-gnu"
+CHOST="x86_64-pc-linux-gnu"
 
 # Host and optimization settings 
 # ==============================
@@ -104,6 +104,7 @@ USE="
 #CFLAGS="-mcpu=athlon-xp -O2 -pipe"
 #CFLAGS="-march=pentium3 -O2 -pipe"
 # Avoid -O0 and -s in CFLAGS.
+CFLAGS="-march=nocona -O3 -pipe -DNDEBUG -fno-stack-check"
 
 # If you set a CFLAGS above, then this line will set your default C++ flags to
 # the same settings.
@@ -260,6 +261,7 @@ PORT_LOGDIR="/var/log/ebuilds"
 #     http://www.gentoo.org/doc/en/rsync.xml
 #
 #SYNC="rsync://rsync.gentoo.org/gentoo-portage"
+SYNC="rsync://rsync.de.gentoo.org/gentoo-portage"
 #
 # PORTAGE_RSYNC_RETRIES sets the number of times portage will attempt to retrieve
 #     a current portage tree before it exits with an error. This allows
@@ -279,7 +281,7 @@ PORT_LOGDIR="/var/log/ebuilds"
 #     specified on every run. Useful options include --ask, --verbose,
 #     --usepkg and many others. Options that are not useful, such as --help,
 #     are not filtered.
-EMERGE_DEFAULT_OPTS="--nospinner"
+EMERGE_DEFAULT_OPTS="--nospinner --verbose"
 #
 # INSTALL_MASK allows certain files to not be installed into your file system.
 #     This is useful when you wish to filter out a certain set of files from
@@ -291,6 +293,7 @@ EMERGE_DEFAULT_OPTS="--nospinner"
 #     the number of parallel makes (-j) to perform. The suggested number
 #     for parallel makes is CPUs+1.
 #MAKEOPTS="-j2"
+MAKEOPTS="-j5"
 #
 # PORTAGE_NICENESS provides a default increment to emerge's niceness level.
 #     Note: This is an increment. Running emerge in a niced environment will
@@ -575,3 +578,4 @@ SSL_UNIT="Preliminary usage"
 # Network-mounted "/usr/portage/distfiles" from some different machine
 # in the LAN; looking there first before downloading from the internet.
 #GENTOO_MIRRORS=""
+GENTOO_MIRRORS="http://212.219.56.134/sites/www.ibiblio.org/gentoo/ ftp://ftp.wh2.tu-dresden.de/pub/mirrors/gentoo ftp://linux.rz.ruhr-uni-bochum.de/gentoo-mirror/ http://linux.rz.ruhr-uni-bochum.de/download/gentoo-mirror/ ftp://212.219.56.135/sites/www.ibiblio.org/gentoo/ http://212.219.56.135/sites/www.ibiblio.org/gentoo/ http://ftp.heanet.ie/pub/gentoo/ ftp://ftp.twaren.net/Linux/Gentoo/ http://mirror.uni-c.dk/pub/gentoo/ ftp://140.127.177.15/pub/Linux/Gentoo"
